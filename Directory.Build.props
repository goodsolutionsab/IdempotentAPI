<Project>
  <PropertyGroup>
    <!-- Package metadata for Good Solutions AB fork -->
    <Authors>Simon Bengtsson, Good Solutions AB</Authors>
    <Company>Good Solutions AB</Company>
    <PackageLicenseExpression>MIT</PackageLicenseExpression>
    <PackageReadmeFile>README.md</PackageReadmeFile>
    <RepositoryUrl>https://github.com/goodsolutionsab/IdempotentAPI</RepositoryUrl>
    <RepositoryType>git</RepositoryType>
    <PackageProjectUrl>https://github.com/goodsolutionsab/IdempotentAPI</PackageProjectUrl>

    <!-- Version prefix - patch version is set by CI/CD -->
    <VersionPrefix>3.0.14</VersionPrefix>

    <!-- Enable package generation -->
    <GeneratePackageOnBuild>false</GeneratePackageOnBuild>
    <IsPackable>true</IsPackable>

    <!-- Embed debug symbols and source code for debugging -->
    <DebugSymbols>true</DebugSymbols>
    <EmbedAllSources>true</EmbedAllSources>
    <DebugType>embedded</DebugType>
  </PropertyGroup>

  <!-- Set PackageId using MSBuildProjectName which is available at the right time -->
  <PropertyGroup>
    <PackageId>GoodSolutionsAB.$(MSBuildProjectName)</PackageId>
  </PropertyGroup>

  <!-- Include README in all packages -->
  <ItemGroup>
    <None Include="$(MSBuildThisFileDirectory)README.md" Pack="true" PackagePath="\" Condition="Exists('$(MSBuildThisFileDirectory)README.md')" />
  </ItemGroup>
</Project>
